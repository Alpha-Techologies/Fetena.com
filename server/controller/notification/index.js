const { getNotification } = require("./getNotification");
const {
  updateNotification,
  authorizeNotificationUpdate,
} = require("./updateNotification");

module.exports = {
  getNotification,
  updateNotification,
  authorizeNotificationUpdate,
};
